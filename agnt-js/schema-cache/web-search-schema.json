{
  "description": "Web Search Tool that performs web searches and retrieves web content without opening a browser.",
  "properties": {
    "action": {
      "type": "string",
      "description": "The action to perform: 'search', 'open', or 'fetch'. Defaults to 'search'."
    },
    "url": {
      "type": "string",
      "description": "The URL to open or fetch content from. Required for 'open' and 'fetch' actions."
    },
    "searchQuery": {
      "type": "string",
      "description": "The search query to use for the 'search' action. Required for 'search' action."
    },
    "resultsCount": {
      "type": "integer",
      "description": "The number of search results to return. Defaults to 5."
    }
  },
  "required": [
    "action"
  ]
}