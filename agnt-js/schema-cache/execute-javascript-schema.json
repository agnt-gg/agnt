{
  "description": "A tool to execute JavaScript code within a secure virtual machine environment, with support for asynchronous operations and web API calls.",
  "properties": {
    "code": {
      "type": "string",
      "description": "The JavaScript code to execute. It can include synchronous or asynchronous operations."
    },
    "inputData": {
      "type": "object",
      "description": "An object containing data that will be available within the sandboxed environment during code execution."
    }
  },
  "required": ["params", "inputData"]
}
