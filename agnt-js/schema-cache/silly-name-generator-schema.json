{
  "description": "A tool that generates silly names by combining random adjectives and nouns. It can generate a single name or multiple names based on the input parameter.",
  "properties": {
    "numNames": {
      "type": "integer",
      "description": "The number of silly names to generate. Defaults to 1 if not provided."
    }
  },
  "required": []
}