{
  "description": "Random Number Generator Tool that generates a random number within a specified range (min to max, inclusive).",
  "properties": {
    "min": {
      "type": "integer",
      "description": "The minimum value of the range. Defaults to 1 if not provided."
    },
    "max": {
      "type": "integer",
      "description": "The maximum value of the range. Defaults to 100 if not provided."
    }
  },
  "required": []
}