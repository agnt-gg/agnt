# Review 3 repairs — batch 4

These are production-import regression repairs, not live acoustic qualification.

- `chatStreamReducer.final_content` replaces different draft text, including an empty final, and replaces text parts while keeping tool parts. Identical finals preserve their interleave. Corrected finals render after retained tool parts. Serializer/hydrator equality is tested; actual store/server/rendered-UI roundtrip remains pending.
- `requestVoiceBridge` rejects conflicting supplied conversation, execution, request, account, user and provider fields, plus terminal message IDs and explicit non-completed outcomes. Already accepted facts remain accepted on later failure. Current server emits `done {message:'Stream ended'}` without account/request attestation; receipt is explicitly labelled `binding:'stream-local'`. Caller expected bindings are supported, but full server echo/authenticated account binding and host integration are NOT established here.
- `speechOut.speak()` resolves `{ok,reason}`; unavailable/throw/timeout/zero-audio failures cannot mark a chunk done. Heard progress starts on `SpeechSynthesisUtterance.onstart` or media `onplaying`, ends only after matching start/end, and is frozen on error. Partial word progress is still an elapsed-playback estimate, NOT independent ASR or proof a human heard it. Cancel settles even if a synth emits no event or fetch ignores AbortSignal. Timeout is bounded to at most 300 seconds, default 120. A provider failure after playback starts never replays through fallback.
- Codex narrator surfaces typed failures through actionable UI messages; native generative media remains muted.
- Pause/resume tombstones uncommitted input and records paused turn IDs. After a pause, an unknown final cannot dispatch without a fresh observed turn creation. This is intentionally fail-closed; provider buffer-clear/liveness on the actual service is not yet qualified. Already dispatched tasks are not cancelled by input invalidation.

Evidence is in `voice-seven-hour-20260908-rLeVR2/evidence/batch-4/`, with failures retained. Five separately targeted mutants are isolated copies; the independent frozen gate itself was never edited. No live provider, GPU, microphone, OS speaker, production service restart or history mutation was used in batch 4. Settings scope, other host adapters, real persistence, owner-admitted local streaming and selected-model post-gate PCM dogfood remain required before publication.
